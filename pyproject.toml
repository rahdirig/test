[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jfrog-frogbot-repro"
version = "0.1.0"
description = "Reproduction project for FrogBot SCA discrepancy"
authors = [{name = "Test"}]
dependencies = []
